
# Ensure Vercel uses the right Node version
engine-strict=true
# Use legacy peer dependencies to reduce build issues
legacy-peer-deps=true
# Ensure node-gyp uses Python 3
python=/usr/bin/python3
# Add this line to help with directory loading issues
node-linker=hoisted
